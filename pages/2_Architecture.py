import streamlit as st

st.set_page_config(
    page_title="Архитектура Приложения",
    page_icon="🏗️",
    layout="wide"
)

st.title("🏗️ Архитектура Приложения \"Калькулятор Клиноременных Передач\"")
st.write("---") # Разделитель

st.header("1. Обзор Архитектуры (Высокоуровневый)")
st.markdown("""
Приложение построено по модульному принципу и следует паттерну "Модель-Представление-Контроллер" (MVC) в упрощенном виде, что характерно для Streamlit-приложений. Основные компоненты:
* **Модуль Данных (`data.py`):** Хранит все статические справочные данные (коэффициенты, стандартные размеры).
* **Модуль Расчетов (`calculations.py`):** Содержит основную бизнес-логику и формулы для выполнения инженерных расчетов.
* **Модуль Приложения/Интерфейса (`1_Калькулятор.py`):** Обеспечивает пользовательский интерфейс, собирает входные данные, вызывает расчеты и отображает результаты.
* **Взаимодействие:** `1_Калькулятор.py` импортирует данные из `data.py` и функции из `calculations.py`.
""")

st.code("""
graph TD
    User -- Ввод данных --> App[1_Калькулятор.py (Интерфейс & Контроллер)]
    App -- Вызов функций --> Calculations[calculations.py (Модель: Логика Расчетов)]
    Calculations -- Использует --> Data[data.py (Модель: Справочные Данные)]
    App -- Отображение --> User
""", language="mermaid") # Если хочешь, чтобы Streamlit попытался отрисовать mermaid-диаграмму. Если нет,
# можешь просто вставить как текст.

st.header("2. Детальная Архитектура Модулей")

st.subheader("2.1. Модуль Данных (`data.py`)")
st.markdown("""
**Назначение:** Единое хранилище всех статических справочных данных, необходимых для расчетов. Это "база знаний" калькулятора.
... (Вставь сюда остальное описание для data.py)
""")

st.subheader("2.2. Модуль Расчетов (`calculations.py`)")
st.markdown("""
**Назначение:** Инкапсулирует всю математическую и инженерную логику калькулятора. Независим от пользовательского интерфейса.
... (Вставь сюда остальное описание для calculations.py)
""")

st.subheader("2.3. Модуль Приложения/Интерфейса (`1_Калькулятор.py`)")
st.markdown("""
**Назначение:** Точка входа в приложение. Ответственен за взаимодействие с пользователем, сбор данных, вызов расчетных функций и представление результатов.
... (Вставь сюда остальное описание для app.py, но адаптируй имя файла на 1_Калькулятор.py)
""")

st.header("3. Взаимодействие между модулями")
st.markdown("""
* `1_Калькулятор.py` является "оркестратором". Он вызывает `calculations.py` для выполнения всех вычислений.
* `calculations.py` получает сырые или предварительно обработанные данные от `1_Калькулятор.py` и обращается к `data.py` для получения всех справочных значений.
* `data.py` является пассивным хранилищем, не содержит логики, только данные.
""")

st.header("4. Используемые технологии:")
st.markdown("""
* **Python:** Язык программирования.
* **Streamlit:** Фреймворк для быстрого создания веб-интерфейса.
* **`math` модуль:** Для математических операций (pi, sqrt, asin, ceil).
* **Структуры данных:** Списки, словари для хранения данных и результатов.
""")

st.write("---")
st.info("Эта архитектура обеспечивает модульность, расширяемость и простоту поддержки приложения.")